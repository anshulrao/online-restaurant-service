package client.user;

/**
 * The three operations that the user performs: (1) view the menu, (2) place
 * orders and (3) check the status of the order.
 */
public enum UserOperation {
  VIEW, ORDER, CHECK
}